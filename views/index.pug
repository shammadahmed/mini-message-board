extends layout

block content
  h1= title
  p Welcome to #{title}
  section
    a(href='/new') Create a new message
    each message in messages
      ul
        each item in message
          li= item
